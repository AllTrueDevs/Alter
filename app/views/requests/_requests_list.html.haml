%div{ class: 'requests'}
  - if requests.size.zero?
    %h5.text-center Запитів не знайдено
  - else
    - requests.each do |request|
      = render partial: 'requests/request_in_list', locals: { request: request, requests: requests, ajax: defined?(ajax) ? ajax : nil }
