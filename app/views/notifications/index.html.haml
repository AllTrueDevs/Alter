= content_for :title, 'Сповіщення'
= content_for :styles do
  = stylesheet_link_tag 'index_show'
.container
  .notification-index
    = render partial: 'notifications/notifications_list', locals: { notifications: @notifications, destination: 'notification' }
