.panel.panel-default.width-100
  .panel-heading
    .row
      .col-md-10
        %h4 Сповіщення
      .col-md-2
        .pull-right
          = link_to clean_notifications_url, 'data-placement': :bottom, title: 'Видалити всі сповіщення.' do
            %span.glyphicon.glyphicon-trash{ 'aria-hidden': true }
  .panel-body
    .notifications
      - if @notifications.empty?
        %h5.text-center Сповіщень не знайдено
      - else
        = render partial: 'notifications/notifications_list', locals: { notifications: @notifications }