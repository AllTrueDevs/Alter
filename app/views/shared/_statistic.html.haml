- if items.size.zero?
  %h5.text-center Запитів не знайдено
- else
  = bar_chart items.joins(:category).group(:name).sum(:count), discrete: true, max: (items.pluck(:count).max < 4)? 4 : items.pluck(:count).max