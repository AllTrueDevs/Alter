= content_for :title, 'Новини'
= content_for :styles do
  = stylesheet_link_tag 'index_show'
.container
  .article-index
    = render partial: 'articles/articles_list', locals: { articles: @articles }
