%h1 Користувачі
= form_tag(users_path, :method => "get", id: "search-form") do
  .input-append
    = text_field_tag :search, params[:search], class: 'form-control', placeholder: "Введіть ім'я користувача"
    %button.btn.btn-success{:type => "submit"} Знайти

- @users.each do |user|
  %div
    %p= user.name
    = image_tag user.avatar.url
    = link_to 'Show', user_path(user)



