!!!
%html
  %head
    %meta{ content: 'text/html; charset=UTF-8', 'http-equiv': 'Content-Type' }
    %title= "Alter | #{yield(:title)}"
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': true
    = stylesheet_link_tag 'application', 'https://fonts.googleapis.com/css?family=Exo+2:300,500&subset=latin,cyrillic'
    = javascript_include_tag 'application'
    = csrf_meta_tags
    = favicon_link_tag '/assets/favicon.png', :rel => 'shortcut icon'
  %body
    -#= render partial: 'home/preloader' if controller_name == 'home'
    = render partial: 'layouts/header'
    .content.container
      -#-flash.each do |name, msg|
      -#  .alert.alert-info.alert-dismissible
      -#    %button.close{ 'data-dismiss': :alert, type: :button }
      -#      %span{ 'aria-hidden': true } ×
      -#      %span.sr-only Close
      -#    = msg
      = yield
      = yield :plugins
    = render partial: 'layouts/footer'
