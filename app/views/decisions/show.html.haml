%p#notice= notice
%p
  %b Request name:
  = Request.find(@decision.request_id).name
%p
  %b Categories:
  -Request.find(@decision.request_id).required_items.each do |item|
    %p= item.category.name


= link_to 'Підтвердити', @decision, :method => :delete, :data => { :confirm => 'Are you sure?' }
= link_to 'Відхилити', @decision, :method => :delete, :data => { :confirm => 'Are you sure?' }
= link_to 'Back', decisions_path
