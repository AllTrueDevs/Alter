%p#notice= notice
%p
  %b Request name:
  = Request.find(@decision.request_id).name
%p
  %b Categories:
  -Request.find(@decision.request_id).required_items.each do |item|
    %p= item.category.name


= link_to 'Підтвердити', :controller => :decisions, :action => :accept
= link_to 'Відхилити', :controller => :decisions, :action => :deny
= link_to 'Back', decisions_path
