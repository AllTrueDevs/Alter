.panel.panel-default.width-100
  .panel-heading
    %h4 Відгуки
  .panel-body
    -if @decisions.empty?
      %h5.text-center Відгуків не знайдено
    -else
      - @decisions.each do |decision|
        = render 'decisions/decision_in_list', decision: decision, decisions: @decisions
        -#%div
        -#  %p= 'Request name: ' + Request.find(decision.request_id).name
        -#  %p= 'Helper: ' + User.find(decision.helper_id).name
        -#  %p= 'Status: ' + decision.status
        -#  %p Категорії
        -#  - decision.accepted_items.each do |item|
        -#    %span{:class => "label label-#{rand(1..8)}"}= item.required_item.category.name
        -#
        -#  = link_to 'Show', decision
